module.exports = {
    reactStrictMode: false,
    images: {
        domains: ["ui-avatars.com"]
    }
    // Temporary
    // webpack: (config) => {
    //     config.resolve.fallback = { fs: false }

    //     return config
    // }
}
